<?php
echo '<ul class="model-list">';
foreach($definitions as $name => $def) {
    echo sprintf(
        '<li><a href="%s">%s</a></li>',
        $this->url('spiffyadmin/view', array('model' => $name)),
        $this->escape($def->options()->getDisplayName())
    );
}
echo '</ul>';